version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
      - npm rebuild
    postBuild:
      commands:
        - npm run test-ci
  artifacts:
    baseDirectory: /dist/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
